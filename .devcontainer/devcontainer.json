{
    "name": "Morpheus",
    "build": {
        "dockerfile": "Dockerfile"
    },

    "runArgs": [
        "--runtime=nvidia",
        "--gpus",
        "all"
    ],

    "containerEnv": {
        "MORPHEUS_ROOT": "/workspaces/morpheus"
    },

    "postStartCommand": "/opt/morpheus-devcontainer/bin/setup-morpheus-env",

    "initializeCommand": [
        "mkdir", "-p", "${localWorkspaceFolder}/.devcontainer/.conda"
    ],

    "mounts": [
        {"type":"bind", "source": "${localWorkspaceFolder}/.devcontainer/.conda", "target": "/home/coder/.conda"}
    ],

    "customizations": {
        "vscode": {
            "extensions": [
                "eamodio.gitlens",
                "llvm-vs-code-extensions.vscode-clangd",
                "ms-vscode.cpptools",
                "nvidia.nsight-vscode-edition",
                "twxs.cmake",
                "xaver.clang-format"
            ]
        }
    },
    "settings": {
        "C_Cpp.intelliSenseEngine": "disabled"
    }
}
